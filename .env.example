# PocketBase Connection Settings
POCKETBASE_URL=http://127.0.0.1:8090

# Option 1: Authenticate via admin credentials
POCKETBASE_ADMIN_EMAIL=
POCKETBASE_ADMIN_PASSWORD=

# Option 2: Authenticate via a pre-generated auth token (e.g. from an existing session or API key)
POCKETBASE_AUTH_TOKEN=
